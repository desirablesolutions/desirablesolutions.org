import Avatar from "@components/Avatar";
import LinkArrowIcon from '@components/LinkArrowIcon';
import vendor from "@configs/vendor";
import Section from "@views/components/Section";
import Carousel from "react-multi-carousel";

import type { ShowcaseProps } from "@models/typings/Showcase";
import type { ComponentType } from "@typings/Component";


const Showcase: ComponentType<ShowcaseProps> = ({ title, featured, latest, order, heading, cta }: ShowcaseProps) => {


    return (
        <></>

    )
}

export default Showcase