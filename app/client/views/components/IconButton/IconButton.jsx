import { IconButton as MuiIconButton } from '@mui/material/IconButton';



const IconButton = ({ children, ...props }) => {

    
    return (
        <IconButton>
            { children }
        </IconButton>
    )
}
